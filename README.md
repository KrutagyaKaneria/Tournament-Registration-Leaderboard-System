# Tournament Registration & Leaderboard System API

## Setup

```bash
npm install
cp .env.example .env
```

Configure your `.env` with the correct database credentials, then:

```bash
npm run dev
```

The server starts at `http://localhost:5000`. A health check is available at `GET /health`.
